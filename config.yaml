training:
  epochs: 100
  batch_size: 8192
  cache_size: 1024  # does not do anything
  learning_rate: 0.05
  momentum: 0.9
  lambda: 1
  scaling_factor: 300
  save_model: "model.pt"

dataset:
  path: "sample.bin"

logging:
  verbose: true
